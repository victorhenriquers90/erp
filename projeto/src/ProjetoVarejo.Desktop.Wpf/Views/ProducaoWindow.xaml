<UserControl x:Class="ProjetoVarejo.Desktop.Wpf.Views.ProducaoWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="{StaticResource BgApp}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Gestão de produção"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextStrong}" />
            <TextBlock Margin="0,8,0,0"
                       Text="Visão operacional de entradas e saídas de produção no período."
                       FontSize="14"
                       Foreground="{StaticResource TextSoft}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,16,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="De" FontWeight="SemiBold" />
                <DatePicker x:Name="DtDe" Width="140" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="Até" FontWeight="SemiBold" />
                <DatePicker x:Name="DtAte" Width="140" />
            </StackPanel>
            <Button Grid.Column="2"
                    Margin="12,20,0,0"
                    Width="120"
                    Height="36"
                    Content="Atualizar"
                    Click="Atualizar_Click" />

            <UniformGrid Grid.Column="3" Columns="3" Margin="20,0,0,0">
                <Border Style="{StaticResource Card3DStyle}" Margin="0,0,10,0" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Ordens em aberto" Foreground="{StaticResource TextSoft}" />
                        <TextBlock x:Name="LblOrdensAbertas" Margin="0,6,0,0" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card3DStyle}" Margin="0,0,10,0" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Entradas (un.)" Foreground="{StaticResource TextSoft}" />
                        <TextBlock x:Name="LblEntradas" Margin="0,6,0,0" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card3DStyle}" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Saídas (un.)" Foreground="{StaticResource TextSoft}" />
                        <TextBlock x:Name="LblSaidas" Margin="0,6,0,0" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </UniformGrid>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card3DStyle}" Padding="8">
            <DataGrid x:Name="DgProducao" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Produto" Binding="{Binding Produto}" Width="3*" />
                    <DataGridTextColumn Header="Mov. entrada" Binding="{Binding Entradas}" Width="1.2*" />
                    <DataGridTextColumn Header="Mov. saída" Binding="{Binding Saidas}" Width="1.2*" />
                    <DataGridTextColumn Header="Saldo atual" Binding="{Binding Saldo}" Width="1.2*" />
                    <DataGridTextColumn Header="Última movimentação" Binding="{Binding UltimaMovimentacao}" Width="1.8*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
