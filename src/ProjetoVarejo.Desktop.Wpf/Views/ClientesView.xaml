<UserControl x:Class="ProjetoVarejo.Desktop.Wpf.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding CarregarCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Clientes"
                       FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource AppTextDarkBrush}"/>
            <TextBlock Text="{Binding Total}"
                       FontSize="13"
                       Foreground="{StaticResource AppTextMediumBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Busca -->
            <TextBox Grid.Column="0"
                     Text="{Binding Filtro, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="🔍  Buscar por nome ou CPF/CNPJ..."
                     md:HintAssist.IsFloating="False"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     md:TextFieldAssist.HasClearButton="True"
                     md:TextFieldAssist.UnderlineBrush="Transparent"
                     FontSize="13"
                     Height="44"
                     Margin="0,0,12,0"/>

            <!-- Botões -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Novo cliente"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="{StaticResource AppPrimaryBrush}"
                        BorderBrush="{StaticResource AppPrimaryBrush}"
                        Foreground="White"
                        Height="44"
                        Padding="20,0"
                        md:ButtonAssist.CornerRadius="8">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <md:PackIcon Kind="Plus" Width="16" Height="16" VerticalAlignment="Center"/>
                                <TextBlock Text="Novo cliente" Margin="6,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Erro -->
        <md:Card Grid.Row="2" Visibility="{Binding Erro, Converter={StaticResource StringToVisibilityConverter}}"
                 Background="#FFEBEE" Padding="16" UniformCornerRadius="8"
                 Margin="0,0,0,12" md:ElevationAssist.Elevation="Dp0">
            <StackPanel Orientation="Horizontal">
                <md:PackIcon Kind="AlertCircle" Foreground="#BB2B2B" Width="20" Height="20"/>
                <TextBlock Text="{Binding Erro}" Foreground="#BB2B2B" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </md:Card>

        <!-- DataGrid -->
        <md:Card Grid.Row="2" Padding="0" UniformCornerRadius="12" md:ElevationAssist.Elevation="Dp1">
            <DataGrid ItemsSource="{Binding Clientes}"
                      SelectedItem="{Binding ClienteSelecionado}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      RowHeight="52"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{StaticResource AppBorderBrush}"
                      BorderThickness="0"
                      Background="Transparent"
                      Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome"       Binding="{Binding Nome}"     Width="*"   MinWidth="200"/>
                    <DataGridTextColumn Header="CPF/CNPJ"   Binding="{Binding CpfCnpj}"  Width="140"/>
                    <DataGridTextColumn Header="Telefone"   Binding="{Binding Telefone}"  Width="120"/>
                    <DataGridTextColumn Header="Email"      Binding="{Binding Email}"     Width="200"/>
                    <DataGridTextColumn Header="Cidade"     Binding="{Binding Cidade}"    Width="130"/>
                </DataGrid.Columns>
            </DataGrid>
        </md:Card>

        <!-- Loading overlay -->
        <Border Grid.Row="2" Background="#88FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True"
                             Width="48" Height="48"
                             Foreground="{StaticResource AppPrimaryBrush}"/>
                <TextBlock Text="{Binding StatusMessage}"
                           HorizontalAlignment="Center"
                           Margin="0,12,0,0"
                           Foreground="{StaticResource AppTextMediumBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
