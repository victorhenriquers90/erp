<UserControl x:Class="ProjetoVarejo.Desktop.Wpf.Views.RelatoriosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding CarregarCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <Button DockPanel.Dock="Right" Command="{Binding CarregarCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}" Height="40">
                <StackPanel Orientation="Horizontal">
                    <md:PackIcon Kind="Refresh" Width="18" Height="18"/>
                    <TextBlock Text="Atualizar" Margin="8,0,0,0"/>
                </StackPanel>
            </Button>
            <StackPanel>
                <TextBlock Text="Relatorios" Style="{StaticResource SectionHeaderStyle}"/>
                <TextBlock Text="Vendas, produtos e desempenho" Style="{StaticResource SectionSubtitleStyle}"/>
            </StackPanel>
        </DockPanel>
        <md:Card Grid.Row="1" Padding="20" UniformCornerRadius="12" md:ElevationAssist.Elevation="Dp1" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="TOTAL VENDIDO NO PERIODO" FontSize="10" FontWeight="Bold" Foreground="{StaticResource AppTextMediumBrush}"/>
                <TextBlock Text="{Binding TotalVendido, StringFormat=C2}" FontSize="28" FontWeight="Bold"/>
            </StackPanel>
        </md:Card>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <md:Card Padding="0" UniformCornerRadius="12" md:ElevationAssist.Elevation="Dp1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Vendas por dia" FontWeight="Bold" FontSize="15" Margin="16"/>
                    <DataGrid ItemsSource="{Binding VendasPorDia}" AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Dia" Binding="{Binding Data, StringFormat=dd/MM/yyyy}" Width="130"/>
                            <DataGridTextColumn Header="Qtd" Binding="{Binding Quantidade}" Width="90"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C2}" Width="130"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </md:Card>
            <md:Card Grid.Column="2" Padding="0" UniformCornerRadius="12" md:ElevationAssist.Elevation="Dp1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Top produtos" FontWeight="Bold" FontSize="15" Margin="16"/>
                    <DataGrid ItemsSource="{Binding TopProdutos}" AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Codigo" Binding="{Binding Codigo}" Width="100"/>
                            <DataGridTextColumn Header="Produto" Binding="{Binding Descricao}" Width="*"/>
                            <DataGridTextColumn Header="Qtd" Binding="{Binding QuantidadeVendida, StringFormat=N3}" Width="90"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding TotalVendido, StringFormat=C2}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </md:Card>
        </Grid>
    </Grid>
</UserControl>
