<UserControl x:Class="ProjetoVarejo.Desktop.Wpf.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding CarregarCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Clientes" Style="{StaticResource SectionHeaderStyle}"/>
            <TextBlock Text="{Binding Total}" Style="{StaticResource SectionSubtitleStyle}"/>
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0"
                        Text="{Binding Filtro, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Buscar por nome ou CPF/CNPJ..."
                        Height="42"
                        Margin="0,0,12,0"/>

            <Button Grid.Column="1"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Height="42" Padding="20,0">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Add24" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="Novo cliente" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Erro -->
        <Border Grid.Row="2"
                Visibility="{Binding Erro, Converter={StaticResource StringToVisibilityConverter}}"
                Background="#FDECEC" Padding="12" CornerRadius="8"
                BorderBrush="#F3B7B7" BorderThickness="1"
                Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="ErrorCircle24" FontSize="18" Foreground="#BB2B2B"/>
                <TextBlock Text="{Binding Erro}" Foreground="#BB2B2B" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Tabela -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Clientes}"
                      SelectedItem="{Binding ClienteSelecionado}"
                      Style="{StaticResource Win11DataGridStyle}"
                      IsReadOnly="True"
                      BorderThickness="0"
                      Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome"     Binding="{Binding Nome}"     Width="*"   MinWidth="200"/>
                    <DataGridTextColumn Header="CPF/CNPJ" Binding="{Binding CpfCnpj}"  Width="140"/>
                    <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}"  Width="120"/>
                    <DataGridTextColumn Header="Email"    Binding="{Binding Email}"     Width="200"/>
                    <DataGridTextColumn Header="Cidade"   Binding="{Binding Cidade}"    Width="130"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Loading overlay -->
        <Border Grid.Row="2" Background="#88FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="44" Height="44"
                                 Foreground="{StaticResource AppPrimaryBrush}"/>
                <TextBlock Text="{Binding StatusMessage}"
                           HorizontalAlignment="Center"
                           Margin="0,12,0,0"
                           Foreground="{StaticResource AppTextMediumBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
